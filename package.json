{
  "name": "calc-expr",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "ts-node-dev --respawn --transpile-only --exit-child test/eval.ts",
    "build:addon": "node-gyp rebuild",
    "copy-assets": "shx cp -r src/addon dist/src && shx mkdir dist/src/addon/build && shx cp -r build/Release/evalExpr.node dist/src/addon/build",
    "build": "tsc && pnpm build:addon && pnpm copy-assets",
    "prepack": "pnpm build"
  },
  "devDependencies": {
    "@types/bindings": "^1.5.5",
    "@types/node": "^20.0.0",
    "node-gyp": "^11.3.0",
    "shx": "^0.4.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [],
  "author": "mfbasta",
  "license": "MIT",
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^8.5.0"
  }
}
